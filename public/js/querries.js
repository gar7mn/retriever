//add the database connection const
import connection from '../../db'

import app from  '../../app'

//should let me call app related commands from here.

app.get('/search',(req,res)=> res.render({
    //idk yet
}))